#!/usr/bin/python
# -*- coding: UTF-8 -*-

class Activity:

    def __int__(self, bank_name, num_consumption, total_account, each_account, register, need_points, weblink, start_date, end_date):
        self.bank_name = bank_name
        self.num_consumption = num_consumption
        self.total_account = total_account
        self.each_account = each_account
        self.register = register
        self.need_points = need_points
        self.weblink = weblink
        self.start_date = start_date
        self.end_date = end_date
